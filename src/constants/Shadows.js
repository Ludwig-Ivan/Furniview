import {StyleSheet} from 'react-native';

const shadow = StyleSheet.create({
  shadowColor: '#000',
  shadowOffset: {
    width: 0,
    height: 8,
  },
  shadowOpacity: 0.44,
  shadowRadius: 10.32,

  elevation: 16,
});

export default {shadow};
